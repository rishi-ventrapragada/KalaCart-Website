# Architectural & Engineering Decisions

*Expanded reasoning behind Section D of CLAUDE.md.*

- **2026-09-07 · Typed Data Seam over Direct Supabase Calls**
  - *Context:* Companion Android app is simultaneously modifying the shared schema.
  - *Decision:* Build UI against `src/lib/data/` interfaces implemented by `mockProvider` with latency simulation.
  - *Why:* Decouples frontend development from backend deploy cycles; prevents schema instability from breaking UI work.

- **2026-09-07 · Inquiry Linkage over Cart/Checkout Flow**
  - *Context:* Indian artisans often deal with made-to-order, custom, or bulk craft requests.
  - *Decision:* Omit cart and payment gateways. Use verified inquiry modal and direct communication channels.
  - *Why:* Matches the SIH26090 requirement and avoids regulatory/logistical overhead of direct e-commerce transactions.

- **2026-09-07 · 200-Line File Limit & Strict Composition**
  - *Context:* Large monolitihic components degrade maintainability and challenge LLM context windows.
  - *Decision:* Keep all source files under ~200 lines by aggressively extracting hooks and subcomponents.
  - *Why:* Keeps files focused, testable, and prevents context degradation during development.

- **2026-09-07 · Scoped Motion System (Home Route Only)**
  - *Context:* Heavy scroll-driven animations can slow down catalog browsing and administrative work.
  - *Decision:* Run the full motion system and Lenis-driven progress on Home only. Rest of site uses lightweight scroll reveals.
  - *Why:* Preserves 60fps performance and high usability across data-dense views.

- **2026-09-08 · Hero Motion is Type, Colour and Lateral Travel, not Parallax Depth** *(Increment 7)*
  - *Context:* PRD 10.2 specified a layered parallax hero: tinted backdrop, far and mid craft-silhouette planes, a floating product-card mockup, and a pinned front silhouette the card emerged from behind (the "front-occlusion signature move"). It was built in full and verified at 284 assertions.
  - *What happened:* Rejected on review. Three further directions were built and shown:
    1. **Layered jharokha arches** — rejected. The motif read as Rajasthani *architecture*, but the product is craft *objects*; it was heritage-generic rather than about the goods. The owner also removed the floating card, which took the front-occlusion effect with it, leaving four planes drifting ~77px — technically correct and emotionally flat.
    2. **The loom** (warp threads struck on load, weft drawn across by scroll, cloth building under the copy) — rejected. The mechanic worked, but at hero scale it read as graph paper rather than as cloth.
    3. **Kinetic type + material field + craft rail** — accepted.
  - *Decision:* The hero's motion is the headline assembling, a dye underline drawing under one marked word, and a category rail travelling laterally against the vertical scroll. No depth planes.
  - *Why:* Four planes sliding at slightly different rates is the most conservative device in the scroll vocabulary — nothing *happens*, you only notice it if you look for it. Lateral travel moves in a direction the reader is not producing themselves, which is what actually reads as alive. It also uses the dye palette (PRD 9.3) that was otherwise sitting unused in the token file.

- **2026-09-08 · No Illustrated Craft Motif in the Hero** *(Increment 7)*
  - *Context:* PRD 10.2 asked to "choose one craft motif and commit" — block-print border, jharokha arches, or pottery/loom shapes.
  - *Decision:* None of them. The hero ground is texture only: a bias-woven grain at 45°/135°.
  - *Why:* Every illustrated option tried read as decoration laid over the page rather than as the subject. A drawn motif also dates badly against the real product photography arriving in Increments 8 and 10, and competes with the Fraunces headline that PRD 9.4 makes the page's voice. Texture gives the canvas a surface without introducing a subject that argues with the type.
  - *Also rejected under this heading:* large blurred dye washes. At hero scale a soft colour bloom reads as a **lighting effect sitting on top of the page**, not as material, and it dirtied the canvas either side of the copy.

- **2026-09-08 · Decoration Yields to Legibility Where They Overlap** *(Increment 7)*
  - *Context:* Making the woven grain visible enough to read as texture pushed `muted` body copy to ~3.3:1 in light and ~3.9:1 in dark — both under the 4.5:1 AA floor the Increment 5 contrast pass established.
  - *Decision:* Mask the grain away from the reading column rather than thinning it globally. Full strength across the open side of the frame, faded to nothing under the copy.
  - *Why:* Thinning the threads far enough to clear AA everywhere left nothing visible at all — the fix destroyed the feature. Masking keeps both: the texture reads where there is nothing to read, and yields exactly where words are. Measured after: 4.59:1 light, 6.24:1 dark for body copy; 10.09:1 for the headline.

- **2026-09-08 · Explicit `transition: none` on Every Scroll-Driven Property** *(Increment 7)*
  - *Context:* PRD 10.2 warns to split `transition` from `transform`. Verification found `transition-property: all` computed on the rail and the blooms.
  - *Decision:* Scroll-driven elements declare `transition: none` explicitly, never merely inherit a zero duration.
  - *Why:* The CSS initial value for `transition-property` is `all`. The duration happened to be `0s`, so nothing lagged *yet* — but any inherited duration anywhere above those elements would have silently reintroduced a full-beat lag behind the scroll, and it would have been very hard to trace back.

- **2026-09-08 · Public Numbers Come From Buyer Reads, Not the Admin Summary** *(Increment 8)*
  - *Context:* The Home impact band initially showed `getAnalyticsSummary().totalProducts`, which is 30. Browse will show the 23 approved products.
  - *Decision:* Count products and traditions from `getProducts()` (approved only); take only the inquiry total from the summary.
  - *Why:* The summary is an admin view and includes pending and rejected rows. A public band asserting 30 crafts next to a catalogue of 23 is a factual error, and on a ministry programme page that is worse than a slow extra request. Applies to any future public stat.

- **2026-09-08 · Remote Images Cannot Break a Card** *(Increment 8)*
  - *Context:* Product and artisan imagery is served from a remote host (picsum now, Supabase Storage later), so a 404 or a blocked request is normal rather than exceptional. A bare `<img>` that fails collapses to its alt text and drags the card's layout with it.
  - *Decision:* All remote imagery renders through `RemoteImage`. The wrapper owns the aspect ratio and paints a tonal placeholder; the image sits on top and is removed on error.
  - *Why:* Verified by blocking the image host entirely — all 8 cards keep their geometry, titles stay readable, no horizontal overflow. The placeholder also shows while the image is in flight, so there is no layout shift on arrival.

- **2026-09-08 · Rail Travel Maps to Real Overflow, Not a Fixed Fraction** *(Increment 8)*
  - *Context:* The craft rail used a fixed fraction of its overflow, tuned at 1280px. At 360px the rail is over four viewports wide: the first card started off-screen to the right (x=468) and the last was never reachable.
  - *Decision:* Map the section's progress onto the rail's actual overflow, clamped, so the first card is flush on entry and the last is reached on exit at any width.
  - *Why:* A phone reader otherwise arrives at an apparently empty strip and can never see the later crafts. Now asserted in the harness at both widths so it cannot regress.

- **2026-09-08 · Mock Behaviour Is Disclosed in the UI, Not Just in Code** *(Increment 10)*
  - *Context:* The inquiry form's success toast was planned as "Your message has been sent", with a code comment marking the write as MOCK.
  - *Decision:* The copy reads "Inquiry saved. In the full version this reaches the artisan directly."
  - *Why:* `createInquiry` writes to an in-memory store that resets on reload. Nothing reaches any artisan. A judge or ministry reviewer seeing the site demoed takes on-screen copy at face value and has no way to read a code comment, so the honest disclosure has to be where they are looking. Applies to the mocked admin auth in Increment 12 too.

- **2026-09-08 · Focus-on-Invalid Must Not Query the DOM Synchronously** *(Increment 10)*
  - *Context:* The inquiry form focused the first invalid field with `querySelector('[aria-invalid="true"]')` inside the submit handler. Verification found focus stayed on the submit button.
  - *Decision:* Derive the first invalid field from the validation result's field order, then index into the form's controls.
  - *Why:* The handler runs before React re-renders, so `aria-invalid` is not on the DOM yet and the query returns nothing. The fields were correctly marked and correctly focusable; only the timing was wrong, which is exactly the kind of defect that looks fine in a screenshot and fails for a keyboard user.

- **2026-09-08 · Hand-Rolled SVG Charts, Not Recharts** *(Increment 15)*
  - *Context:* CLAUDE.md §C listed Recharts in the stack and PRD 11.8 names it explicitly for the analytics bar and line charts. It was never actually installed — `package.json` has no charting dependency at all, so Increment 15 was the point where the library either arrived or was dropped.
  - *Decision:* Drop it. Both charts are inline SVG, built from the `AnalyticsSummary` the seam already returns. This supersedes §C and PRD 11.8.
  - *Why:* Three reasons, in order of weight. **Bundle:** Recharts pulls a set of D3 sub-packages, roughly 90 kB gzipped, to draw a six-row bar list and a nine-point line on a single admin-gated page no buyer ever loads — the whole current dependency tree is smaller than that. **Theme integration:** the entire design system is CSS variables swapped under `[data-theme]`, and inline SVG inherits `var(--accent)` and the per-category dye tones for free, in both themes, with no re-render. Recharts takes colours as JS props, so dual-theme support would mean reading resolved variables in JS and threading them through `ResponsiveContainer` — a wrapper whose only job is to undo the library's assumption that colour is static. **Size:** the charts are ~60 lines of SVG each, comfortably inside the 200-line law, so the library was not saving meaningful work either.
  - *Trade-off accepted:* no tooltips or animated transitions out of the box. Neither is required by 11.8, and the accessible `<table>` fallback each chart card carries gives exact values without hover — which serves a keyboard or screen-reader user better than a tooltip would.
  - *Revisit if:* a later increment needs genuinely interactive charts (brushing, zoom, dense time series). Two static charts is not that.

- **2026-09-08 · The Acceptance Criteria Are a Committed Script, Not a Habit** *(Increment 16)*
  - *Context:* PRD 14 lists eleven global acceptance criteria. Through fifteen increments they were checked by hand, or by a Playwright harness in `lab/` that was gitignored and thrown away after each use. Increment 16's whole deliverable is the claim "everything built so far still holds", which is exactly the claim nobody can check by reading a commit.
  - *Decision:* `audit/` is committed and run with `npm run audit`. It sweeps every route at 360/768/1024/1280 in both themes, plus a reduced-motion pass, and exits non-zero on failure.
  - *Why:* A disposable harness verifies one moment and then stops existing; the next person to touch a component has no way to know what it used to guarantee. Committing it turns the acceptance criteria into something executable, and it found a real defect on its first full run (see below) that fifteen increments of manual checking had missed.
  - *Design:* `checks.mjs` splits `snapshot()` (runs in the page, measures only) from the assertions (run in Node, judge only), so every judgement is readable in one place rather than buried in a string serialised into a browser. `motion.mjs` holds the reduced-motion checks. Adding a check is one exported function plus one line in the runner.
  - *Deliberately excluded:* anything about whether a page looks right. The sweep proves contrast, overflow, focus and heading order; both themes still need a human pass. A harness that claimed to check taste would be trusted for something it cannot do.

- **2026-09-08 · Rendered Geometry, Not Class Presence** *(Increment 16, restating Increment 7)*
  - *Context:* The reduced-motion craft-rail check could have asserted that `.craft-rail` carries `overflow-x: auto`. The Increment 7 dead-scroll bug would have passed that check while the rail sat motionless.
  - *Decision:* The sweep scrolls the last card into view and asserts its measured bounding box is actually on screen.
  - *Why:* The class was always present in that bug; what was wrong was the computed geometry (`scrollWidth === clientWidth` on a `w-max` element). A check that asserts intent rather than outcome will pass for exactly the bugs that matter. Worth applying to any future check of a scroll-driven or layout-dependent behaviour.

- **2026-09-08 · Loading Is Derived, Never Set** *(Increment 16)*
  - *Context:* Adding a retry to the craft rail's error state, the obvious implementation called `setState('loading')` at the top of the effect. oxlint's `react(set-state-in-effect)` flagged it.
  - *Decision:* Store the attempt a result belongs to, and derive `loading` by comparing it against the current attempt - the trick `useAsyncData` already uses.
  - *Why:* Setting state synchronously in an effect starts a second render pass on every run. The rail hand-rolls its fetch because the drift ref makes the hook awkward, and in doing so it had quietly diverged from the pattern the hook exists to enforce. Any component that hand-rolls a fetch should copy the attempt-comparison, not the naive version.

- **2026-09-10 · The Palette Revision: `secondary` Becomes a Red, and Sage Is Parked** *(recorded retroactively 2026-09-11)*
  - *Recorded late.* This entry reconstructs a change that shipped without one. Its reasoning survived only as code comments in `theme.ts`, `buttonStyles.ts` and `Badge.tsx`, which is why the resulting 4-vs-5 gap between `Dye` and `dyes` later read as accidental drift and prompted a fresh investigation. Reconstructed from those comments and the measured values in them; the *decisions* are recovered accurately, but no session notes survive, so treat the framing as inferred.
  - *Context:* A reference palette arrived carrying a sage green. The open question was which semantic token it should take, with `secondary` the apparent fit as the only unassigned slot.
  - *Decision:* Sage takes **no** semantic token. `secondary` becomes madder (`#A63A3A` light, `#C85C5C` dark), and sage is parked in `dyes` as a fifth entry with no consumer.
  - *Why sage cannot be `secondary`, on two independent grounds:* semantically, `secondary` is not decorative — it is the invalid-field border (`fieldStyles`), the error-toast border (`Toast`) and the destructive fill behind "reject artisan" (`buttonStyles`). It means *something is wrong*, and a calm green cannot carry that in any of the three. Numerically, sage measures **2.28:1** as a border on canvas, under the 3:1 functional-boundary floor — so it would have failed the contrast bar even if the semantics had worked. Madder measures 5.37:1 on canvas and 6.29:1 on card.
  - *Where sage went, and why that is not an endorsement:* `dyes` is the one block with **no contrast floor**, which is what made it the only slot sage could occupy. Placement by elimination, not by intent. It was deepened from the reference `#8DA38A` to `#7E9679` so it reads as a dye rather than a pastel on parchment. **It is deliberately absent from `Dye` in the data seam**: that union is the contract with the teammate's Supabase schema, and admitting a tone no category has would commit us to an undefined seventh craft.
  - *Knock-on 1 — the destructive button label inverted.* `destructive` was `text-ink` and correct while `secondary` was a mid-tone amber and `ink` a dark brown (5.09:1). **Both ends moved** in this revision: `secondary` became a dark red and `ink` a dark espresso, which put a dark label on a dark fill at **2.13:1** — the worst pairing on the site. The fill is dark in both themes now, so the label follows `primary` and goes `canvas`: 5.99:1 light, 4.88:1 dark. The lesson is that a token swap has to be re-measured against the tokens it *pairs with*, not only against surfaces.
  - *Knock-on 2 — a collision retired, but the fix stays.* Light `secondary` was previously `#B8862F` against `marigold` `#C9922B`, so pending and rejected badges read as the same amber in the admin artisan table (Increment 14) — a table whose whole job is telling them apart. Making `secondary` a red retires that collision outright. `Badge`'s `negative` tone nevertheless **stays `madder`**, now for a different reason than it was first set: `madder` is a *dye*, one fixed value in both themes, whereas `secondary` is theme-dependent and is now the "something is wrong" token that will keep moving with the destructive fill. A status badge should not inherit those shifts. Worth noting as a pattern — when the cause of a workaround disappears, the workaround still needs its own justification to stay, and here it had one.
  - *Unchanged by design:* `secondary` remains barred from text everywhere. It measured 3.24:1 on light card and 2.69:1 on dark as a text colour (Increment 5), so `ErrorState` copy and field error messages stay `ink` and the tone lives in the border. The revision changed the hue, not that rule.

- **2026-09-10 · Real Product Photography Is Allowed in the Hero** *(Increment 17)*
  - *Context:* A rotating 3D image-spiral was proposed for the hero, beside the headline, showing real product photos. Two standing decisions appeared to forbid it: "No Illustrated Craft Motif in the Hero" (texture only) and "Decoration Yields to Legibility Where They Overlap".
  - *Decision:* Adopt it. The spiral renders real listings from the data seam, in its own column to the right of the copy.
  - *Why:* The earlier rejection was of *drawn subjects*, on two grounds that do not transfer. First, an illustration is a second subject arguing with the Fraunces headline for the page's voice; photography of the goods is the page's actual subject, the same frames Browse renders. Second, that decision explicitly rejected illustration for "dating badly against the real product photography arriving in Increments 8 and 10" — this *is* that photography, so the objection now argues for it. The legibility rule is untouched because nothing overlaps: the spiral is a grid sibling of the copy, not a layer behind it, and the grain mask still clears the reading column.
  - *Scope:* Texture-only still governs the hero **ground**. `MaterialField` is unchanged.
  - *Also decided:* The hero gains **no** loading or error state. The headline column paints independently, so first paint and LCP are unaffected; the spiral is absent until its data lands and renders nothing at all on error. A deliberate exception to the three-states rule on the grounds that this is decoration, not a data view — the same reasoning that makes `RemoteImage` swallow a dead image rather than surface it.
  - *Also decided:* Front-of-spiral positions carry object-forward crops (blue pottery, Madhubani, brassware); scene-like frames (a weaver at a loom, a vendor in a stall) sit toward the back where the edge blur abstracts them. Checked by eye at render size: the scene frames dissolve into indistinct texture below about 110px, and block printing at 88px is a plain blue rectangle. This is an ordering choice within the existing pool — no fixture changed, and Browse and the featured grid, which render at full card size, are untouched.

- **2026-09-10 · The Hero Spiral Is the One Continuous rAF Loop Outside Lenis** *(Increment 17)*
  - *Context:* The adopted component (`InfiniteSpiral`, external) shipped its own rAF loop *and* a `window` scroll listener. The listener was attached unconditionally — the `animationMode` check sat inside the handler — so restricting the mode would have left the listener in place, merely inert.
  - *Decision:* The scroll listener is deleted outright, not disabled. The rAF loop stays, because it is not scroll-linked: it advances on its own clock and never reads scroll position, so it cannot drift out of phase with the Lenis singleton the way a second scroll source would.
  - *Why:* The motion discipline forbids a second *scroll source*, not a second animation. A clock-driven loop on Home, the one route with the motion budget, is in policy; a second reader of scroll position is not, because it would sample raw `window.scrollY` while everything else reads the Lenis-smoothed value.
  - *Also decided:* Under `prefers-reduced-motion` the loop **never starts** — `requestAnimationFrame` is not called once. The source zeroed its speed and kept the loop alive forever, writing transform, opacity, filter and zIndex to every card every frame to produce a static picture. Verified by counting rAF calls in the page: 0 under reduced motion, ~180 per 1.5s otherwise.
  - *Also decided:* The subtree is `aria-hidden` with no roles, no `alt` text and nothing focusable. The source announced eight list items labelled "Spiral image 1…8" and put rotating links in the tab order — focusable targets that move away and can sit at `opacity: 0`. Generated alt text of that kind passes the sweep's `img has alt` check while being worse than silence, which is why §I step 9 requires checking accessibility by hand.

- **2026-09-10 · Spiral Card Separation Is a Geometric Invariant, Not a Tuning Choice** *(Increment 17)*
  - *Context:* Cards overlapped as they came round the front of the helix — one image sliding across the one before it instead of floating into place. Measured across a full rotation: up to **67% overlap, on every single frame**.
  - *Cause:* Two independent errors, both inherited from the adopted component's defaults. 132px cards were rising only 58px between neighbours, so they overlapped by 74px before anything moved; and the perspective term was clamped at 1.45, letting the front card inflate to 183px against that same 58px gap — a 125px overlap at the front of the turn. The horizontal swing could never rescue it: the swing is a sine, so twice per turn it passes through zero, and at exactly those moments vertical spacing is the only separation there is. Its peak (130px) was in any case below the 132px card width.
  - *Decision:* `verticalSpacing` must be at or above `cardHeight × centerScale × DEPTH_SCALE_MAX`. Shipped as 112px cards, 140px spacing, centreScale 1.08, depth clamped to 1.12 (invariant: 140 ≥ 135.48). Verified 0.000000 worst-case overlap across the full cycle at four container widths, and 0 on real rendered boxes over 40 browser samples.
  - *Why an invariant rather than tuned numbers:* the failure looks like a design choice. Overlapping cards read as a deliberate stack, so nobody files it as a bug — it went out in the first pass unnoticed. `hasCardSeparation()` is exported from the transform module and asserted at module load in development, so a later change to spacing, card size or centre scale fails loudly at the source instead of surfacing weeks later in a screenshot.
  - *Cost accepted:* centre emphasis drops from 1.18 to 1.08 and cards from 132px to 112px. The front card is less dominant than the first pass, which is the price of never colliding; the depth read now comes from the blur and fade rather than from scale alone.
  - **⚠️ SUPERSEDED 2026-09-10 by "Spiral Overlap Is the Depth Cue" below. The premise — that overlap is the failure — was wrong.**

- **2026-09-10 · The Navbar Hairline Is Structure, Not a Scroll Affordance** *(Increment 17)*
  - *Context:* On Home at rest the header had no bottom edge. The border was inside the glass-flip ternary (`border-transparent` until ~40px of scroll), so the chrome bled into the page — conspicuous once the hero ground carried a full-strength texture running right up under it.
  - *Decision:* `border-b border-line-strong` is unconditional; only the fill and blur flip on scroll. PRD 10.5 asks for a gradient-to-glass transition, which is a statement about the *fill*, not about whether the header has an edge.
  - *Blast radius, checked rather than assumed:* `Navbar` is rendered once in `AppShell` and so is shared by every buyer route (Browse, product, artisan, 404); the admin desk has its own `AdminLayout`. But off-Home routes never subscribe to the scroll engine, so `onMotionRoute` is false and they were **already** pinned to the solid `border-line-strong` state — as is Home under reduced motion. The only state this actually changes is Home-at-rest with motion on.

- **2026-09-10 · The Acceptance Sweep Cannot See Texture Contrast** *(Increment 17)*
  - *Context:* Darkening the hero ground, the question was whether the result still cleared the AA floor. The sweep came back 64/64 green with a chunky knit at full strength sitting directly under the closing CTA's centred body copy — a combination that visibly degraded the text in a screenshot.
  - *Cause:* `checkContrast` resolves an element's background through `effectiveBg`, which walks up the tree for the first painted `background-color`. Every texture on this site is a `background-image` on `.material-field__grain`, an absolutely-positioned **sibling** of the copy — not an ancestor, and carrying no background-color of its own. So the walk passes straight over it and compares the text against the flat section canvas. The checker is not wrong about what it measures; it simply never sees the texture.
  - *Consequence:* **a green sweep is not evidence that a textured ground is legible.** It never was, for any of them. The `3.3:1` figure in the 2026-09-08 hero-grain entry could not have come from the sweep either — it must have been sampled by eye or by pixel, which is worth knowing when re-reading that entry.
  - *Decision:* Texture work is verified by worst-pixel sampling of the rendered page, and the sweep is treated as covering structure only. The method: screenshot the surface at the mask alpha the copy actually sits at, take the **darkest local 4x4 average** in the texture band (the worst ground a glyph can land on), and compute the WCAG ratio against the text colour. That is deliberately stricter than WCAG, which assumes a flat background — a texture has no single background, so the worst case is the honest one.
  - *Measured baseline (light, `muted` #6f6559), on the real page:* hero copy behind the mask **4.58:1** with the default weave and **4.32:1** with the deep weave; closing bookend **4.54:1**. Dark measures 6.71:1 everywhere and is unaffected by weave weight, because the brass grain never renders darker than the charcoal canvas — the canvas itself is the worst case.
  - *A prediction that was wrong, and why it matters:* these numbers were first estimated from a standalone swatch sheet that modelled the mask as a flat `opacity`, which gave 4.60:1 for the hero and 3.47:1 for an unmasked deep weave. The real page applies `mask-image` as a **gradient that ramps across the paragraph's width**, so the darkest ground under the copy is well right of the midpoint the flat model assumed. Estimating from a synthetic swatch is not verification; sample the actual rendered surface.
  - *Not fixed in the checker, and why:* teaching `effectiveBg` to composite sibling textures means resolving stacking order and gradient maths in the page — a large change to the one part of the sweep whose judgements are deliberately kept simple and readable on the Node side. The cheaper correction is knowing the gap and covering it by hand, which is what this entry is for. Worth revisiting if textures spread beyond these two surfaces.

- **2026-09-10 · The Hero Ground Is the Deep Weave; the Bookend Keeps the Default** *(Increment 17)*
  - *Context:* The hero's woven grain was too faint to read as material. The ask was more visibility, first as a knit ("like a knit sweater"), then — after seeing it — as the original weave carried darker to the same visibility.
  - *What was tried and rejected:* A knit is a different structure, not a louder weave, so it was prototyped properly: chevron/V-arm lattices (read as argyle netting or isolated darts), and offset soft-shadow loop gradients with wale columns (read as knitted fabric, and in dark genuinely well). The loop version shipped briefly. In light it read as **quilted upholstery** rather than wool — the shading is uniform, so it looks pressed rather than yarn-like — and it was reverted at the owner's call.
  - *Decision:* Keep the original bias weave and carry it in a heavier yarn: `--grain-deep-warp` 26%, `--grain-deep-weft` 20%, thread 1.25px, on the hero only. Dark uses brass at 30%/22% for the same reason the default does — ink is the light value there, so a darker thread would vanish.
  - *Why these numbers, and how they were chosen:* by measuring mean ink coverage (luminance drop against bare canvas), not by eye — thin dense threads and sparse soft loops are not visually comparable at equal token values. Against the knit's drop of 15.65: the shipped default reaches only **25%**, which quantifies why it looked faint; 26%/20% at 1.25px reaches **98%**. A darker-but-thinner variant (34% at 1px) hits the same **102%** coverage but the diagonals harden into a grey mesh — **the cloth read is carried by thread weight, not by ink**, which is the transferable finding here.
  - *Why per-surface:* `MaterialField` serves the hero and the closing bookend, and they have opposite exposure. The hero's mask clears its reading column; the bookend centres its copy in the open middle where that same left-to-right mask has already faded to ~0.94, so a heavier weave there would sit at full strength directly under 16px `muted` text. So the component gained a `weight` prop and the bookend keeps the default, measured unchanged at **4.54:1**.
  - *The cost on the hero, and how it was resolved:* the first `deep` shipped at 26%/20% @1.25px and took the hero's body copy from 4.58:1 to **4.32:1** — under the 4.5 AA floor. A box-over-full-strength-texture redesign was then built and reverted at the owner's call (it worked — 16.46:1 headline, 5.70:1 subhead, zero texture bleed — but the plain ground was preferred). The resolution instead was to **let the floor set the weave weight**: sweeping candidates and taking the heaviest that still clears 4.5 gives **13%/10% @0.9px = 4.547:1**. The cloth is lighter than 26/20 and that is the price of the margin.
  - *Method note, because the first two sweeps lied:* a run injecting `background-image` on `.material-field__grain` never applied (a more specific rule won) and silently re-measured the shipped weave ten times. A second run overrode the tokens correctly but took the single darkest 4×4 block, which jittered ±0.04 and returned non-monotonic ratios across monotonically lighter weaves — physically impossible, and the tell that it was noise. Averaging the **darkest 0.5% of blocks** at 1px stride gives a spread of 0.000 across repeated runs. Assert the property actually changed, and prefer an averaged tail over a single extremum.
  - *If more cloth is wanted later:* the lever is the **mask**, not the weave — widening its clear zone rightward buys margin without thinning the threads — or the reverted card treatment, which removes the trade entirely.
  - *An invalid measurement, recorded so it is not repeated:* a tuning run that injected candidate weaves via `addInitScript` to find a lighter passing weight produced eight rows spanning 4.29–4.36 — including for the default, which independently measures 4.58. A hash of the computed `background-image` was identical on every row, proving the injected stylesheet never applied and the harness re-measured the shipped weave eight times. When sweeping candidate styles in-page, **assert that the property actually changed** before trusting any number that comes back.
  - *Verification:* worst-pixel sampling, not the sweep — see the blind-spot entry above.

- **2026-09-10 · Spiral Overlap Is the Depth Cue; the Invariant Is Paint Order** *(Increment 17)*
  - *Context:* The shipped spiral read thin and spread out — cards drifting with large gaps rather than packing into a helix. The suspicion was that `responsiveRadius`/`fitScale` was shrinking too hard for the hero column, or that too few products were being fed in.
  - *Neither was the cause.* `fittedRadius` only binds on narrow columns and governs the horizontal swing, not the card-to-card gap; and the item count cannot tighten density at all, because `cardTransform` places cards at `offset × verticalSpacing` — a ninth card extends the helix further up and down rather than packing the existing eight. There were 30 fixtures available, so the pool was never the constraint.
  - *Actual cause, found by diffing against the source.* The original was pulled from the reactbits shadcn registry (`https://reactbits.dev/r/InfiniteSpiral-TS-CSS`) and compared line by line. Our port is identical **except one constant**: the depth clamp, `(0.72, 1.45)` in the source, narrowed to `(0.8, 1.12)` here by the superseded decision. That one change did all the damage — it flattened the front-to-back size ratio from 1.63x to 1.37x, and the `verticalSpacing` 60 → 140 that accompanied it spread 8 cards over **980px of travel inside a 544px box**, so barely half the helix was ever visible behind the mask.
  - *The merge was real, but it was never a spacing bug.* Two cards genuinely appeared to fuse at the front of the turn. The cause is structural and is **inherited from the source**: depth is `cos(angle)`, and cosine is symmetric, so a card `+k` positions from centre and one at `−k` sit at *exactly* the same z — on every frame, forever. Depth alone cannot order them, and the source broke the tie with `+ index`, i.e. array position: arbitrary, unrelated to helix position, and it flips as cards wrap. Measured across a full cycle: **9 mis-painted frames per 2000** under the source's own scheme. Because it strikes only scattered frames it reads as a stutter rather than a static mistake, which is how it survived a screenshot review and got misfiled as "cards are colliding, push them apart".
  - *Decision:* Restore the source's density and depth clamp — radius 165, 104px cards, `verticalSpacing` 56, 7 per turn, centre scale 1.2, depth `(0.72, 1.45)`, 10 cards — and fix the tiebreak the source got wrong. `depthOrderKey()` resolves equal depths by **centrality** (how near the middle of the visible run a card is), which is the property the eye actually reads as nearer, and is continuous in `offset` so the order slides instead of popping.
  - *The invariant is replaced, not deleted.* `hasCardSeparation()` asserted "cards must never overlap" — the wrong contract, and the thing that flattened the spiral. `frontCardIsClear()` asserts what matters: **the front card is never painted underneath a card it overlaps.** It lives in `src/lib/home/spiralInvariant.ts` (verification, not geometry — which also keeps `spiralTransform.ts` under the 200-line law at 202/82) and still throws at module load in development.
  - *Verified on the live DOM, not in simulation:* over 240 sampled frames — **0 front-card occlusions**, **60.5% peak neighbour overlap** (source ≈61%), **1.55x depth ratio** (was 1.36x), **504px spread** inside the 544px column (was 980px). Reduced motion re-checked: **0 rAF calls** with all 10 cards positioned, vs 397 normally. Acceptance sweep 64/64, unchanged from baseline. Both themes captured and confirmed via `data-theme`.
  - *The lesson worth keeping:* a visual symptom and its cause can sit in different parts of the pipeline. Overlapping cards *looked* like a geometry problem, so geometry got changed — and the fix removed the effect the component exists to produce while leaving the actual defect (paint order) untouched. When adopting an external component under §I, diff the maths against the source before retuning its constants; a constant that differs from the original is a likelier culprit than the design.

- **2026-09-11 · The Impact Band Sits on a Contrasting Ground, and the Two Themes Invert in Opposite Directions** *(Increment 19)*
  - *Context:* The ask was a "dark strip from the theme's colors" behind "The programme so far", to act "only as a solid color background for that section". It is not only a background: `ImpactBand` was styled entirely for the page ground, so the strip breaks four things at once.
  - *The palette had no token for this.* Grepping for an existing dark-surface pattern turned up only `bg-accent text-canvas` on buttons — an accent fill, not a surface. The dye tones are theme-independent mid-tones (PRD 9.3) and `--ink` is a text colour. So this is a genuinely new colour, which under §I.4 needs a decision entry before it ships.
  - *Decision:* Add the pair `--surface-contrast` / `--on-surface-contrast`, hexes in `theme.ts` per law 3, aliased in `@theme inline`.
  - *Why a PAIR and not one colour — the load-bearing part:* the band means **contrasting**, not dark, and the themes move in opposite directions. Light goes darker than its parchment canvas (`#2A1D16`); dark goes **lighter** than its charcoal one (`#242424`), because a dark strip on `#121212` is a band nobody can see. Light therefore has to invert its text and dark does not. A component painting on this ground cannot name `ink` or `canvas` and be correct in both themes, and putting a per-theme conditional in the component would be the first such conditional on the site — the seam's whole premise is that a component names a role and the theme resolves it. `--on-surface-contrast` absorbs the direction in the palette: `canvas` in light, `ink` in dark. `ImpactBand` writes one class.
  - *Why `#2A1D16` and not `--ink`:* reusing `ink` as the fill would put the section's own default text colour at **1.00:1** against its ground — the one pairing guaranteed to be invisible — and a band that is merely ink-coloured reads as a heavy paragraph rather than a change of surface. One step deeper reads as a surface and gives `canvas` copy 13.72:1.
  - *Why `#242424` and not `--card`:* lifted one step past `card` (`#1C1C1C`) so the band still separates where the two meet — 1.207:1 against canvas, 1.098:1 against card — without becoming a second card. Luminance only, no hue, matching the reasoning that keeps this theme's cards neutral so photography stays the only saturated thing on the page.
  - *Measured consequences, every one of which needed a fix:* `ink` reads **1.20:1** on the light band and `muted` ~1.9:1, so the stats take the band's colour, with labels at 70% of it — one value to keep in step rather than a second tone. Measured on the rendered page at **6.64:1** light and **7.01:1** dark, not the ~8:1/~9:1 the palette arithmetic predicted: the alpha composites in oklab, so estimating it as a straight sRGB blend overshoots. Both still clear the 4.5 floor comfortably. Resolving `oklab(… / 0.7)` by parsing the computed string is a trap — the three numbers are ~0–1 and read as 0–255 give a nonsense 1.28:1; let the browser flatten it with `color-mix` and read back `rgb()`. `accent` reads **2.97:1** and is **barred from this surface**, which is why `ImpactBand` passes `SectionHeader` no `linkTo`: that link is `text-accent` and would fail silently. `Skeleton`'s `bg-line` measures **11.64:1** here — a placeholder that should recede instead reads as a *bright* bar — so it drops to the band's colour at 15%.
  - *`ErrorState` gained an opt-in rather than a restyle:* it hardcodes `text-ink` (1.20:1 here) and a `bg-card` retry capsule that reads as a near-white blob on the band. It has **nine other call sites**, all correctly on the page ground, so a global change would have been wrong at eight of them. A `tone` prop defaulting to `page` leaves them untouched; `contrast` swaps the copy colour and trades the filled capsule for an outline in the band's own colour, keeping the 999px control shape (PRD 9.5) and colour-only hover.
  - *The sweep genuinely covers this one.* Unlike every texture on the site, this ground is a painted `background-color` on an **ancestor** of the copy, so `effectiveBg` resolves it correctly and the contrast pass really is evidence here. The 2026-09-10 texture blind-spot entry does **not** apply — worth stating explicitly, because the two grounds look similar in the markup and the wrong assumption would be silent in either direction.

- **2026-09-11 · The Artisans Directory Is Its Own Route, and It Is Filterable** *(Increment 20)*
  - *Context:* Home's "See all artisans" pointed at `/browse` — a question about *makers* answered with a grid of *products*. The reported symptom was that one link. The actual gap is that the site had no artisan-first surface at all: `/artisan/:id` existed for a single profile, but nothing listed them, so a buyer who wanted to shop by person had no entry point. PRD 11 names no such route, which is why this is a decision and not a fix.
  - *Decision:* A new buyer route at `/artisans` listing every approved artisan, with `Artisans` as a peer of `Browse` in the navbar, the mobile menu and the footer.
  - *Why a route and not a section on Browse:* Browse is `ProductFilters` end to end — its URL contract (PRD 11.3 fixes those param names), its sort options and its price range are all properties of *products*. An artisan list has none of those and needs `region` to mean "where this person works" rather than "where this product came from". Bolting it on would have meant a mode flag threaded through the filter hook, the panel and the results grid, so that every one of them carried a branch for a case only half of them care about.
  - *Why it is filterable, which is the part that was not asked for:* the ask was "all their profiles in one page", and with five fixtures a plain roster satisfies it. But `getAllArtisans` **already** accepts `query`, `categoryId`, `region` and `status`, so the filters cost one hook and one component rather than any seam work — and the page's whole purpose is answering "who makes the thing I want, near me", which is a filter query. A roster that is pleasant at five makers and unusable at eighty is a surface that has to be rebuilt precisely when the programme succeeds.
  - *`useArtisanFilters` is a sibling of `useProductFilters`, not a widening of it:* that hook parses a price range and a sort and returns a `ProductFilters`. Serving both surfaces would need a mode flag and two shapes of return value — more coupling than the ~30 lines it saves. What the two genuinely share is the **pattern** (the URL is the state, empty string deletes a param, typing replaces while a deliberate pick pushes), and a pattern is cheaper to repeat than to abstract. Its `craft` param is spelled for the reader; Browse's `category` predates it and PRD 11.3 fixes that name, so it is not ours to change there.
  - *Status is not a URL param.* The route pins `status: 'approved'`, so no hand-typed query string can surface pending or rejected artisans on a buyer page. Same principle as the 2026-09-08 "public numbers come from buyer reads" entry: moderation state is admin data, and a buyer surface should not be one param away from leaking it.
  - *The regions facet reads unfiltered, deliberately.* If the dropdown were built from the current result set, picking a region would remove every other option and strand the reader with no way back except the browser's own back button. Same reasoning and same shape as Browse's facets read.
  - *`showCount` is opt-in rather than always on.* `productCount` was already on the `Artisan` type and already returned by `getAllArtisans` — Home simply never rendered it. It stays off there: five cards are a taste of the makers, and a count invites comparing people against each other on a section whose copy is about their work being their own. On the directory it is the one fact that helps a buyer choose whose profile to open. It also renders nothing when `undefined`, so a provider that does not compute it cannot put "undefined listings" on a card.
  - *Two empty states, not one.* No-match (a filter is applied, Clear is the way out) and nothing-listed-yet (the catalogue is genuinely empty, so offering to clear nothing would be a control that does nothing). The distinction matters most at launch, when the second is the likely one and the first would misdescribe it.
  - *The filters are one inline row, not Browse's sidebar-and-drawer.* Three controls over a flat list of people do not earn a column, and a drawer below `lg` would hide the only means of narrowing the page behind a button. The row wraps to one control per line at 360px.
  - *Verified:* `npm run audit` at **72/72** — `/artisans` is added to the sweep's route list, which is what takes it from 64, so the new route is checked at four widths in both themes like every other one. Filter round-trip, the singular/plural count, the Clear affordance and both empty states driven in a real browser; light, dark and 360px eyeballed, since the sweep does not judge looks. The texture blind spot does not apply here — this page paints `background-color` throughout.

- **2026-09-11 · The Closing CTA Offers Both Entry Points, Weighted** *(Increment 20)*
  - *Context:* With `/artisans` shipped, the site has two ways in — by craft, or by the person who made it — but the closing bookend still offered only the catalogue.
  - *Decision:* Both, side by side. Browse keeps the filled accent capsule; Artisans takes the `secondary` outline.
  - *Why they are not peers:* two solid accent fills beside each other leave a reader with no primary action — the hierarchy collapses and the pair reads as a segmented control rather than as an invitation plus an alternative. PRD 9.6 also rates this bookend as a large surface for accent, so doubling the filled area works against a rule the palette already applies here. The outline still reads as a real button; it is simply the second one.
  - *Why a new i18n key rather than reusing `nav.artisans`:* a capsule at the end of a page is an invitation, and the bare noun "Artisans" reads as a tab label in that position. `home.closing.ctaArtisans` says "Meet the artisans", matching the register of "Browse crafts" beside it.
  - *They stack below `sm`.* A 360px line cannot hold two `lg` capsules without one wrapping its label mid-word; the stack goes full-width so the two are equal rather than ragged.
  - *Verified:* sweep still 72/72, both hrefs confirmed on the rendered page, and the section checked by eye in light, dark and at 360px — the `bg-card` outline sits cleanly over the textured bookend ground in both themes.

- **2026-09-11 · The Taxonomy Comes From the Real Supabase Table, Minus Three Rows** *(Increment 21)*
  - *Context:* The mock taxonomy was six invented crafts (`Handloom Textiles`, `Blue Pottery`, `Madhubani Painting`, `Brassware and Dhokra`, `Block Printing`, `Bamboo and Cane`) chosen before the backend existed. With the Supabase connector authorized, the live `categories` table could finally be read: **12 rows**, with `id`, `name`, `icon`, `image_url`, `created_at`. The mock and the backend named different crafts, which is a contract problem the data seam exists to prevent.
  - *Decision:* Take the real names, ship **nine** of the twelve, and leave the DB untouched.
  - *Two rows are a duplicate seed, not a taxonomy.* "Bamboo Craft" and "Textiles" were inserted at `07:19:38`, fifty-four minutes after the other ten at `06:25:25`, and duplicate "Bamboo" and "Handloom" respectively. The table's `UNIQUE` constraint is on `name` and cannot catch them — they are distinct strings for the same craft. Shipping all twelve would put two Bamboo cards in the craft rail and two Bamboo options in every category filter, which a demo reviewer reads as a bug rather than a taxonomy. They collapse into their originals here.
  - *"Home Decor" is dropped because it is a use-case, not a craft.* Every other row answers *what is it made of*; this one answers *where does it go*. A brass lamp, a terracotta planter and a block-printed cushion are all home decor, so it cuts across Metal Art, Pottery and Handloom simultaneously. That is also exactly why it has no dye tone: the grouping is by material family, and a category with no material family has no tone to take. The alternative — a fifth tone — would reopen the deliberate `sage` gap, which the 2026-09-10 palette entry parks in `dyes` precisely so nothing claims it.
  - *The DB is not modified.* Deduplicating a shared backend is the teammate's call, and the Android app reads the same table. The divergence is recorded here and in `categories.ts` so the gap reads as deliberate rather than as drift — the same failure mode the palette entry was written retroactively to fix.
  - *Ids stayed put wherever the real name is a WIDENING of the craft that id already held* — c2 Blue Pottery → Pottery, c4 Brassware and Dhokra → Metal Art, c3 Madhubani Painting → Paintings. That is what let thirty products, nine artisans and five photo pools carry over with no fixture churn at all. New crafts take fresh ids from c7.
  - *`c5` is retired and its fixtures absorbed into `c1`.* Block Printing is textile work, so `p17`-`p20` and artisan `a5` (Bagru) moved to Handloom, and the six block-print frames were **merged into c1's pool rather than deleted** — they are eye-verified Indian textile photography and Handloom legitimately covers them. c1's pool is now twelve frames, which only widens gallery variety.
  - *Pottery moved indigo → brass.* `c2` was indigo because *Blue* Pottery is literally cobalt. The widened name no longer justifies a colour taken from one glaze, and fired earthenware groups with metal on material. The cost is the rail losing an indigo card to Handloom alone; the consistency is worth more than the distribution. Brass now carries four of nine, which is lopsided but is simply what material-family grouping yields.
  - *`icon` is modelled, `image_url` is not.* `icon` is populated in all twelve live rows, so it joins the `Category` type. `image_url` is `null` in ten rows and empty string in two — never once a URL — so a field for it would be a promise the data cannot keep. Added when it carries URLs.
  - *Four crafts ship with no fixtures, and that is safe by construction.* Wood Craft, Jewellery, Leather and Stone Art have no products, artisans or photo pools. `gallery()` is keyed on a **product's** `categoryId`, never on the category list, so an empty craft never reaches it — verified by a clean production build. The moment a product names one, the 2026-09-10 hard throw fires at import with the offending id named. That guardrail is now live rather than theoretical, which is what it was built for. Fixtures land one craft at a time under the same eye-verified standard.
  - *`OBJECT_FORWARD` did not change.* It stays `['c2','c3','c4']`: the ids kept their pools, so the object-versus-scene finding is about the same frames it always was, and the absorbed block-print frames joined c1, which was already classified as scenes. Only the comment needed rewriting to the new names. The four empty crafts never reach the spiral, and each needs classifying by eye when its frames land.
  - *The "six crafts" copy became count-free rather than "nine".* `home.products.body` sits a few hundred pixels from `ImpactBand`, which counts **traditions actually carrying an approved listing** — five while fixtures are still landing, not nine. Two different numbers for the same thing on one page reads as an error, so the line now says "across India's craft traditions", which is true at every fixture count and needs no edit as crafts gain content.
  - *Verified:* `npx tsc --noEmit` clean, production build clean (proving no module-scope throw), `npm run audit` at **72/72**, and the rail screenshotted in both themes at 1280px — nine cards, correct names, dye groups visually legible, no duplicate Bamboo and no Home Decor. An earlier sweep run failed with a `networkidle` timeout on `/browse`; that was a transient network episode, confirmed by probing baseline and branch three times each (~1.5s, 23 images, zero failed requests on both) rather than assumed.

- **2026-09-11 · The Auth Bridge Plan Inverts Its Own §3: Do Not Mint `auth.users`** *(planning only — nothing applied)*
  - *Context:* Open item #2 in the handoff asks for the `profiles.auth_user_id` backfill that unblocks item #3. `memory/auth-migration-plan.md` §3 step 2 says to *create* the five real users through the Admin API, keyed on email, because when it was measured on 2026-09-10 `auth.users` held exactly one row and the Android app authenticated against Firebase. Both premises expired between that measurement and today: the handoff records that the Android app migrated to Supabase Auth mid-project, and that the 13 test-fixture profiles were deleted.
  - *The call:* the user-creation step is **struck from the sequence** and replaced by a detection query (phase 1d/1c of `auth-bridge-execution-plan.md`). If the app has already registered these five people — which is what "migrated to Supabase Auth" means — then minting them again through the Admin API produces a *second* `auth.users` row per human. That is not a duplicate record, it is a duplicate *identity*: two logins for one person, and an email match key that stops being unique on the `auth.users` side precisely where the brief's constraint #1 says the whole risk lives. The migration would then attach a profile to whichever row the join happened to return, and the new scoped policies would enforce that mistake faithfully.
  - *Why this is a decision and not a fix:* the plan is not wrong; it is stale in a way that reads as authoritative. Its §3 is written as an imperative with a measured table above it, so the natural failure mode is to execute it, see five rows created, and take that as success. Recording the inversion means the next session reads "do not mint" beside the reasoning rather than re-deriving it from two documents whose dates have to be compared first.
  - *The structural backstop, independent of the above:* `idx_profiles_auth_user_id`, the partial unique index on `auth_user_id where not null` (plan §2, kept). It makes the one-profile-per-login rule a constraint rather than a property of a correct backfill — a wrong `UPDATE` fails loudly instead of silently cross-linking two people. Worth having even if every count comes back clean.
  - *Also recorded:* phase 1 could not be run. There is no Supabase MCP tool in this session and `.env.local` carries only `VITE_SUPABASE_ANON_KEY`, which cannot read `auth.users`, `pg_policies` or `pg_constraint` — so no fallback script exists either, and every number in the plan remains the stale 2026-09-10 set until the connector returns. The same block is why open item #5 (does the `enquiries` CHECK reject an insert carrying neither `buyer_id` nor guest contact fields?) has **no answer yet**: it is settled by `pg_get_constraintdef` plus a `begin/insert/rollback` probe, not by reading the constraint definition alone, because a NULL-tolerant CHECK passes on `UNKNOWN` — which is the exact trap the question is about. See `memory/auth-bridge-execution-plan.md`.

- **2026-09-11 · The Auth Bridge Is Applied, and the Seller Policies Are Two Joins Short** *(phases 1, 2 and 4 live; phase 5 blocked)*
  - *What ran:* the connector came back, so phase 1 (inventory) and phase 2 (match-as-SELECT) executed read-only and phase 4 (backfill) was applied by the owner in the Supabase SQL editor. Five profiles at the start, three `auth.users` rows — all email, all confirmed — and the app had indeed already registered them, which is the confirmation the 2026-09-11 strike above was waiting on. Minting via the Admin API would have produced a second login per person. The struck step stays struck.
  - *The match key collapsed to one candidate.* `profiles.firebase_uid` has been **dropped from the table entirely** by the teammate, and no firebase uid survives in `auth.users.raw_user_meta_data`. That retires the exact-UID join the plan ranked first, and it also quietly removes the rollback and audit trail `auth-migration-plan.md` §3 item 5 explicitly said to preserve — worth knowing, because there is now no way to re-derive the old identity if a mapping is ever disputed. Email was all that was left: three matched exactly, two matched nothing, none matched more than one, and neither side carried a duplicate. The gate passed on its own terms rather than by judgement.
  - *`auth_user_id` already existed*, with an FK to `auth.users` and a **full** unique index rather than the partial `where not null` one this plan specified. With zero rows populated the difference was moot — Postgres treats NULLs as distinct under a plain unique index, so multiple unbridged profiles were legal either way — and the "stop and reconcile" gate did not trip, because that gate turns on the column being *populated*. It was empty. Recorded because it is pre-existing work that neither document mentions.
  - *The orphan call was the owner's, and it was a third option.* Not "leave unmapped" and not "mint", but **delete**: Ananya and Nidhi were removed and will re-register through the app. The FK cascade took two `sellers` rows and one `buyers` row with them. This was safe **only because `products` was empty** — the same decision against a populated catalogue would have destroyed real listings, so it is not reusable precedent. Population (b), auth users with no profile, turned out not to exist at all.
  - *The finding that blocks phase 5:* `auth.uid()` cannot be compared to `products.seller_id`. They sit two joins apart — `auth.users.id = profiles.auth_user_id`, then `profiles.id = sellers.profile_id`, and only then does `sellers.id` meet `products.seller_id`. The three live policies "Seller inserts / updates / deletes own product" test `seller_id = auth.uid()` **directly**, comparing a login uuid to a seller-row uuid. They have never matched a row and never could, which is the concrete reason the permissive `*_all` policies are the only thing granting the Android app access today. They are marked for **replacement, not reuse** — dropped by name and recreated against `current_seller_id()`, which resolves both joins in one place. This generalises: anything whose `seller_id` references `sellers(id)` owes both joins (`orders` in phase 6 has the same shape), while `verification_requests.seller_id` references **`profiles(id)`** and therefore needs `current_profile_id()` instead. Same column name, two meanings, and the wrong helper fails exactly as silently.
  - *Why phase 4 is still not finished:* `unmapped = 0` proves the UPDATE wrote what it was told to. It does not prove `auth.uid()` resolves for a real request, and the half that was actually broken — the client sending only the anon key — is invisible to every SQL-side check. The real-JWT verification is the teammate's, through the Android app. Until it returns, applying phase 5 against a client with no JWT denies every request on all six tables.
  - *Open item #5, closed.* `enquiries_buyer_or_guest_check` **rejects** an insert carrying neither `buyer_id` nor guest contact fields, established by a rolled-back probe insert rather than by reading the constraint definition — a NULL-tolerant CHECK passes on `UNKNOWN`, which was the entire trap in the question. So the guest-contact work from `20260910072905` has no gap: the database cannot hold an enquiry with no way to reach anyone.
  - *The question that has to be answered before anyone is told to re-register:* there is no trigger creating a `profiles` row on `auth.users` insert. If the Android app does not do it itself at signup, Ananya and Nidhi **cannot come back** — they would land an `auth.users` row with no profile, no seller row, and no route to listing products, with their old rows already cascaded away. And if the app does create the profile but leaves `auth_user_id` NULL, they return unbridged and every scoped policy is inert for them again. See `memory/auth-bridge-execution-plan.md`.

- **2026-09-12 · The Admin Queue Calls Supabase Directly, Bypassing the Single Swap Point** *(Increment 13 wiring)*
  - *Context:* `src/lib/data/index.ts` is the documented swap point (PRD 5.2): every component imports from it, and moving to the real backend is meant to be one line, `const provider: DataProvider = supabaseProvider`. The verification queue is the first surface that needs real data, because approving an artisan or a product has to write to the shared database the mobile app reads.
  - *Decision:* the queue route imports the four queue methods from `supabaseProvider` directly. `index.ts` is left pointing at `mockProvider`, and the public site — Home, Browse, product and artisan pages — keeps serving mock data. Only `getPendingArtisans`, `getPendingProducts`, `setArtisanStatus` and `setProductStatus` are implemented against Supabase; the other seven `DataProvider` methods stay as the `notWired` stub.
  - *Why:* `products` currently holds **zero** rows. Flipping the seam today would empty the public site — every listing, every category page, the analytics summary — and trade a populated demo for a blank one. The queue is the opposite case: it *wants* the real table, because the rows it must show are the pending submissions the mobile app writes, and the whole point of approving one is that a buyer on the phone sees it. Mock data cannot demonstrate that; only the shared database can.
  - *What this costs:* the swap point stops being the single integration point while this holds, which is exactly the property it was created to have. Two providers are live at once and a reader of `index.ts` can no longer tell where the admin data comes from. That is the deliberate trade, not an oversight.
  - *When it is reversed:* once real product content exists, the seam flips to `supabaseProvider` for the whole site and the direct import in the queue route is deleted. That requires the remaining seven methods to be implemented first. Separate scope, not started.
  - *Also recorded, because it is the reason this is not a complete auth story:* these writes succeed today only because RLS is wide open on `products`, `sellers` and `profiles` — the permissive `*_all` policies noted in the 2026-09-11 entry above. Anyone holding the publishable key can approve or reject anything. The admin gate (Increment 12) remains a mock gate by design. Wiring the queue does not change either fact, and neither should be read as solved by this entry.

- **2026-09-12 · The SPA Rewrite Excludes Root Assets by Negative Lookahead, and the `$` Anchors Are Load-Bearing** *(rescued from the rewritten README)*
  - *The config:* `vercel.json` rewrites `/((?!favicon\.ico$|favicon\.svg$|apple-touch-icon\.svg$|og-image\.svg$|assets/).*)` to `/index.html`. A client-rendered router needs deep links like `/browse` and `/artisans` to reach the app shell rather than 404, which is what the rewrite is for; the lookahead carves out the things that must *not* be treated as routes. The four named files are exactly what `public/` holds at its root.
  - *What the naive version broke:* the source used to be `/(.*)` with no exclusions, so **every** path returned the app shell with `200 OK`, including paths that matched no real file. Browsers request `/favicon.ico` by convention whether or not a page declares one. The browser therefore asked for an icon, got an HTML document, and — never having been told the icon was absent — went on displaying whichever favicon it had cached from a previous visit. The bug presented as a stale favicon, i.e. as a caching or CDN problem, and not as a routing problem, which is the whole reason it is worth keeping the mechanism written down rather than only the corrected pattern.
  - *The rule this encodes:* assets must 404 when they are missing; only routes fall through to the shell. A 200 carrying the wrong content type is worse than a 404, because it suppresses the client's own error handling — the browser cannot fall back if it is told the request succeeded.
  - *Why the `$` anchors matter:* they pin each exclusion to an *exact root-level* path. `/deep/favicon.ico` therefore still resolves as an SPA route instead of punching a hole in the rewrite. Without the anchors the exclusion would match any path merely *ending* in those names, and arbitrary nested routes would start 404ing — the inverse failure, and a much more confusing one to diagnose because it would only affect some URLs.
  - *The maintenance consequence:* this pattern is a hand-maintained allowlist. Any new root-level static file has to be added to it, or it will be served the app shell and fail in the same near-invisible way the favicon did. That coupling between `public/` and `vercel.json` is not enforced by anything, which is the standing risk.
  - *Why it is recorded now:* it was only ever written in `README.md`. That file was rewritten on 2026-09-12 for buyers and reviewers rather than for maintainers, so the explanation survived solely in git history and in a regex that does not explain itself. See `vercel.json`.

- **2026-09-12 · Two Unrelated Histories in One Repository, and the Red `app` Build Is Correct** *(originally commit `beecb45`, README-only)*
  - *The layout:* `main` is the default branch and holds this website; Vercel builds and deploys it. `app` holds the Android app, FastAPI backend and SQL, pushed from the separate `KalaCartJava` working tree. They are genuinely unrelated — `git merge-base main origin/app` returns nothing, so there is no common ancestor at all.
  - *Why that matters operationally:* the ordinary branch reflexes are all wrong here. `app` is not "behind" `main`, it must never be merged or rebased into it, and a diff between the two is noise rather than a changeset. Anything that assumes a shared root (a comparison view, an automated sync, a well-meant `git merge origin/app`) will either fail or produce a catastrophic mess.
  - *The expected failure:* a push to `app` produces a **failing Vercel preview build**, because that branch carries no `package.json` — verified, not assumed: `git cat-file -e origin/app:package.json` reports the path missing from the tree. Production is untouched, since Vercel builds `main` only in production.
  - *The trap:* a red build is a strong prompt to fix something, and there are two tempting non-fixes — adding a `package.json` to `app`, or wiring that branch into the site's build config. Both would be wrong. The correct response is to leave it red. Ignoring the preview build for `app` is the intended state, and this entry exists so nobody "repairs" it.
  - *Deploy trigger, for the same reason:* deploys come from the Vercel GitHub App on push to `main`, not from the CLI. `vercel --prod` from the project root still works for an out-of-band deploy, but it is not what normally ships the site — so a push landing on `main` should be understood as a production deploy.
  - *Why it is recorded now:* commit `beecb45` recorded all of this, but only inside `README.md`, which was rewritten on 2026-09-12. A maintainer seeing a red `app` build would have had to dig it out of a deleted section of a file that no longer discusses branches.
