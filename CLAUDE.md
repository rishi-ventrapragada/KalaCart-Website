# KalaCart — CLAUDE.md
*Last updated: 2026-09-10 · Owner: Rishi Ventrapragada*

---

## A · What this folder is
The web front-end for KalaCart — a direct-linkage buyer marketplace and administrative moderation dashboard supporting Indian artisans (SIH26090, Ministry of Social Justice & Empowerment). It is built against a typed mock data layer first and shares a Supabase backend with a companion Android app.
Stage: active development / pre-launch.

## B · The Goal
- **Why it exists:** Connect rural and traditional Indian artisans directly with conscious buyers and institutions, bypassing middlemen and preserving heritage craft.
- **Done looks like:** A responsive, dual-themed web portal with fluid Home motion, faceted catalog filtering, artisan storytelling, direct inquiry modals, and an admin verification desk, verified across both themes.
- **Out of scope:** Payment gateways, shopping carts, or checkout flows (linkage model only); native device features handled by the Android app.

## C · Stack
- **Languages:** TypeScript (strict mode, no uninspected `any`)
- **Frameworks:** React 19 · Vite · Tailwind CSS v4 · React Router
- **Libraries:** Lenis (smooth scroll) · Lucide React · GSAP (Home only: the maker fan and the featured-grid reveal, see §D). Admin charts are hand-rolled SVG, not Recharts (see §D).
- **i18n is a typed dictionary of our own, not react-i18next** — no i18next package is installed. `src/lib/i18n/en/` holds per-surface segments, `en.ts` assembles them, and `TranslationKey` is derived from that object, so `t('a.typo')` fails the build rather than rendering blank.
- **Lenis is a site-wide singleton, mounted once in `AppShell` for every buyer route** (PRD 10.1) — it is the *scroll engine* (`useScrollEngine`, the hero spiral, the progress bar) that is Home-only. The admin desk is outside `AppShell` and mounts neither. Under `prefers-reduced-motion` no Lenis instance is created at all.
- **Hosting / infra:** Vercel
- **Key services:** Supabase (shared backend via data seam)
- **Run locally:** `npm run dev`
- **Key files:** `PRD.md` · `src/lib/data/` (data seam) · `src/lib/i18n/` · `src/App.tsx` · `audit/` (acceptance sweep)
- **Verify:** `npm run build && npm run preview -- --port 4173`, then `npm run audit` in another terminal

## D · Decisions
*One line each. Date · what · why.*
- `2026-09-07` — Typed mock data provider before Supabase to decouple UI progress from schema shifts.
- `2026-09-07` — Inquiry linkage model over cart/checkout flow per SIH26090 artisan requirements.
- `2026-09-07` — Strict 200-line file cap per component to enforce modularity.
- `2026-09-07` — Semantic CSS variables for dual themes ("Raw Cotton" light, "Gallery Wall" dark).
- `2026-09-07` — Full motion system restricted to Home route; lightweight reveals elsewhere.
- `2026-09-08` — Hero motion is type, colour and lateral travel, not parallax depth. Layered planes read as flat and the illustrated motifs (jharokha arches, self-weaving loom, dye washes) all read as decoration laid over the page rather than as the goods. See `memory/decisions.md`.
- `2026-09-08` — No illustrated craft motif in the hero. Texture only (a bias-woven grain), so nothing competes with the headline or dates against real product photography.
- `2026-09-08` — Decoration yields to legibility where they overlap: the hero grain is masked away from the reading column rather than thinned globally, because thinning enough to clear AA leaves nothing visible.
- `2026-09-08` — Buyer-facing numbers are counted from buyer-facing reads, never from `getAnalyticsSummary()`, which is an admin view including pending and rejected rows.
- `2026-09-08` — Remote imagery renders through `RemoteImage`: the wrapper owns the geometry so a dead image cannot collapse a card's layout.
- `2026-09-08` — Mock behaviour is disclosed in the UI, not just in code comments. A demo reviewer takes on-screen copy at face value and cannot read a comment.
- `2026-09-08` — Admin charts are hand-rolled SVG, not Recharts. Supersedes the Recharts mention in §C and PRD 11.8. Two charts on one gated page do not justify ~90 kB gzipped of D3, and inline SVG reads the theme's CSS variables and category dye tones directly instead of needing a theme-reading wrapper around `ResponsiveContainer`. See `memory/decisions.md`.
- `2026-09-10` — The palette revision: `secondary` becomes madder (a red), and the reference palette's **sage takes no semantic token**. Recorded retroactively on 2026-09-11 — it shipped with its reasoning only in code comments, and the gap it left later read as accidental drift. Sage fails `secondary` twice over: that slot means *something is wrong* (invalid-field border, error toast, reject fill) and a calm green cannot say it, and sage measures **2.28:1** as a border on canvas, under the 3:1 boundary floor. It is parked in `dyes` — the one block with no contrast floor — and so is **deliberately absent from `Dye` in the data seam**, which is the contract with the Supabase schema: `dyes`/`DyeName` (5 tones) is the rendering palette, `Category['dye']` (4) is the data contract, and the asymmetry is intentional. Two knock-ons: the `destructive` button label inverted to `canvas` because **both** ends moved (dark-on-dark hit 2.13:1), and the Increment 14 pending/rejected amber collision is retired — though `Badge`'s `negative` stays `madder` anyway, since a dye is theme-fixed while `secondary` now tracks the destructive fill. See `memory/decisions.md`.
- `2026-09-10` — Real product photography **is** allowed in the hero, as a rotating spiral beside the headline. This reopens the texture-only hero decision on that decision's own grounds: the illustrated motifs were rejected for being *drawn subjects* competing with the headline, and specifically for dating badly against the real photography arriving in Increments 8 and 10. Photography is not a competing subject — it is the goods. It also sits in its own column beside the copy, never under it, so the grain's legibility mask is untouched. Texture-only still governs the hero *ground*. See `memory/decisions.md`.
- `2026-09-10` — GSAP is a dependency, scoped to the Home maker fan (Increment 18, adopted under §I). It is the first package added for a single component, so the scope is the point: the tween engine drives the fan's deal and hover-push, and nothing else on the site imports it. Its easings do **not** come with it — `elastic.out` and `back.out` are overshoot curves the single-easing law forbids, so both are replaced by `CustomEase.create('kalacart-site', '0.22, 1, 0.36, 1')`. That registration is load-bearing, not decorative: GSAP does not parse CSS easing syntax, `parseEase('cubic-bezier(…)')` returns `undefined`, and a tween given an unrecognised ease **silently** falls back to `power1.out` (measured 0.4375 at quarter progress against the real curve's 0.7649). Every tween would have broken the motion discipline while looking correct. See `memory/decisions.md`.
- `2026-09-10` — The maker fan is desktop-only, with the existing grid below `lg`. A fan needs hover to open and overlaps its own cards by design, so on a touch screen it would present five makers with three partly covered and no gesture to reveal them. Both presentations render the same links from the same read.
- `2026-09-08` — Acceptance criteria are enforced by a committed sweep (`npm run audit`), not by memory. Structure, contrast, overflow, focus and reduced-motion are checked on the production build across four widths and both themes; looks still need a human pass. See `audit/README.md`.
- `2026-09-10` — **The acceptance sweep is blind to texture contrast.** Its contrast pass resolves a background by walking up for the first painted `background-color` (`effectiveBg`, `audit/checks.mjs`), so a texture painted as a `background-image` — every `.material-field__grain`, and any future one — is invisible to it: text is compared against the flat canvas behind the texture. A green sweep is therefore **not** evidence that a ground is legible. Any texture change needs worst-pixel sampling of the rendered page (screenshot the surface, take the darkest local average under the copy, compute the ratio) before it ships. Measured this way against `muted`: hero **4.547:1**, closing bookend **4.54:1**; the sweep reports neither and passes 64/64 whatever the texture is. See `memory/decisions.md`.
- `2026-09-10` — The hero ground is the **deep** weave, and its weight is **set by the AA floor rather than by eye**: 13%/10% at 0.9px, the heaviest weave whose worst-pixel ground still clears 4.5:1 against `muted` (measured **4.547:1**). One step heavier fails. An earlier pass shipped 26%/20% at 1.25px — better cloth, but 4.19–4.32:1 — and was pulled back rather than carried as a knowing AA regression. The margin is ~0.05 and the sweep cannot see it, so re-measure by pixel sampling after any change to `muted`, hero type size, or the mask ramp. Dark is unconstrained (brass on charcoal never darkens past the canvas: 6.71:1 at every weight) and keeps its own values. The bookend stays on the default weave at 4.54:1. `MaterialField` takes a `weight` prop; a knit structure was prototyped and rejected (it read as quilting in light). See `memory/decisions.md`.
- `2026-09-10` — The navbar hairline is **unconditional**, not part of the glass flip. At rest on Home the header previously had `border-transparent` and so no bottom edge at all; PRD 10.5 asks the *fill* to change on scroll, not the edge. `Navbar` renders in `AppShell` for every buyer route, but off-Home routes were already pinned to the solid state, so this only changes Home-at-rest. See `memory/decisions.md`.
- `2026-09-11` — The craft rail is a **continuous marquee, not the scroll-driven lateral move**. It loops leftward on its own clock at a constant 42 px/sec and pauses on hover and on focus-within. This retires `useRailDrift` for this section (the hook is now unused): a scroll-linked transform and a continuous animation both write lateral position to the same element and would fight every frame, so the two cannot coexist — one had to go. Home keeps its other scroll-linked moments (hero spiral, maker fan), so the page has not lost its motion identity. The travel is a compositor CSS animation rather than a per-frame transform *because* it needs no scroll value: it reads nothing from the Lenis singleton and adds no second rAF loop (PRD 10.1). Timing is **linear**, the one documented exception to the single-easing law — an infinite loop has no endpoints to ease between, and `cubic-bezier(.22,1,.36,1)` would make the row visibly surge and stall once per cycle. Duration is derived from the measured track width, never fixed, so speed stays constant however many crafts are listed. **Two copies of the track are rendered and the loop translates exactly -50%**, which is what makes the wrap seamless; the duplicate is `aria-hidden` with `tabIndex={-1}` so assistive tech hears six crafts, not twelve. This depends on one copy being wider than the viewport (six cards ≈ 93rem) — if the taxonomy ever shrinks, a copy narrower than the frame would wrap early and open a visible gap. Under reduced motion the loop **stops**: one static copy, no animation, and `.craft-rail-viewport` becomes the hand-scrollable region with snap points (PRD 10.7). That last point moved the scroll container up a level, so `audit/motion.mjs` was repointed from `.craft-rail` to `.craft-rail-viewport` and now reaches the last real card rather than a track wrapper — against the old selector the check would have passed while testing nothing. See `memory/decisions.md`.
- `2026-09-11` — Craft dye reaches the card **surface**, not just its border and dot. Each card's ground is a low-alpha `color-mix` of its dye into `card` (9–11%, deepening to 16–19% on hover), which re-opens the Increment 5 contrast resolution that kept dye out of anything behind label text — on its own terms. That resolution was about *saturated* dye behind copy: the tones are theme-independent mid-tones (PRD 9.3), so filling a card with one puts `ink` on a mid-tone ground and loses AA in at least one theme. Mixed into `card` at this weight the ground stays a near-card tint, the craft is still legible as colour, and the label keeps card-level contrast in both themes. Marigold and brass carry the higher percentages because they are lighter tones and shift the ground less per unit. Note the sweep's texture blind spot does **not** apply here — these are painted `background-color`, which `effectiveBg` resolves correctly, so the 64/64 pass is real evidence for these cards. See `memory/decisions.md`.
- `2026-09-11` — The impact band sits on its own **contrasting ground**, added as the token pair `--surface-contrast` / `--on-surface-contrast`. The band means *contrasting*, not dark, and **the two themes move in opposite directions**: light goes darker than its parchment canvas (`#2A1D16`, deeper than `ink` so the section reads as a change of surface rather than a heavy paragraph), dark goes **lighter** than its charcoal one (`#242424`, lifted past `card`) — because a dark strip on a `#121212` ground is a band nobody can see. That asymmetry is why it is a *pair* and not one colour: the ground inverts in light but not in dark, so a component painting on it cannot name `ink` or `canvas` and be right in both themes. `--on-surface-contrast` resolves the direction in the palette (`canvas` in light at **13.72:1**, `ink` in dark at **13.26:1**), keeping the component declarative and the measured values beside the others in `theme.ts`. Consequences, all measured: `ink` reads **1.20:1** on the light band and `muted` ~1.9:1, so the stats take the band's colour and its 70% for labels (measured on the rendered page: **6.64:1** light, **7.01:1** dark); `accent` reads **2.97:1** and is therefore barred from this surface — which is why `ImpactBand` passes `SectionHeader` no `linkTo`, its link being `text-accent`; `Skeleton`'s `bg-line` measures 11.64:1 here and reads as a *bright* bar instead of an absent one, so it drops to the band's colour at 15%; and `ErrorState` gains an opt-in `tone="contrast"` (outline capsule, not the `bg-card` fill) rather than a global restyle that would wrongly touch its nine other call sites. Unlike the hero grain, this ground **is** a painted `background-color`, so `effectiveBg` resolves it and `npm run audit` genuinely verifies this text — the texture blind spot does not apply. See `memory/decisions.md`.
- `2026-09-11` — Artisans get **their own route** at `/artisans`, listed as a peer of Browse in the navbar, mobile menu and footer. Home's "See all artisans" pointed at `/browse`, answering a question about makers with a grid of products; the deeper gap was that no artisan-first surface existed at all. PRD 11 names no such route, hence a decision rather than a fix. It is **filterable** (search, craft, region) even though "all their profiles in one page" does not ask for that: `getAllArtisans` already accepts exactly those filters, so they cost one hook and one component, and a roster that is pleasant at five makers is unusable at eighty — the rebuild would land precisely when the programme succeeds. `useArtisanFilters` is a **sibling** of `useProductFilters`, not a widening: that hook parses a price range and a sort and returns a `ProductFilters`, so serving both would need a mode flag and two return shapes. Status is **not** a URL param — the route pins `approved`, so no hand-typed query can surface pending rows on a buyer page. The regions facet reads unfiltered, or picking a region would delete every other option. `ArtisanCard` gains an opt-in `showCount` (already on the type, never rendered); it stays off on Home, where five cards are a taste rather than an inventory. Sweep is **72/72**, `/artisans` having been added to the route list. See `memory/decisions.md`.
- `2026-09-11` — The closing CTA offers **both entry points, weighted**: Browse keeps the filled accent capsule, Artisans takes the `secondary` outline. Two solid accent fills side by side leave no primary action and read as a segmented control, and PRD 9.6 already rates this bookend as a large surface for accent. The label is its own key (`home.closing.ctaArtisans`, "Meet the artisans") rather than a reuse of `nav.artisans`, which reads as a tab label in a capsule. They stack full-width below `sm`, where two `lg` capsules cannot share a line without wrapping a label mid-word. See `memory/decisions.md`.
- `2026-09-11` — The craft taxonomy is the **real Supabase `categories` table, minus three rows**. The live table has 12; the mock ships **9**. Two are a duplicate seed inserted 54 minutes after the originals — "Bamboo Craft"/"Bamboo" and "Textiles"/"Handloom", which the `UNIQUE` constraint on `name` cannot catch — and they collapse into their originals. **"Home Decor" is dropped**: it is a use-case, not a craft (a brass lamp, a terracotta planter and a block-printed cushion are all home decor), so it has no material family and therefore no dye tone; a fifth tone would reopen the deliberate `sage` gap. **The DB is left untouched** — deduplicating a shared backend is the teammate's call, and the Android app reads the same table. Ids stayed put where a real name *widens* the craft that id held (c2 Blue Pottery → Pottery, c4 Brassware → Metal Art), so 30 products and 9 artisans carried over with no churn; **`c5` is retired**, Block Printing being textile work, with its fixtures and its six frames absorbed into `c1` (pool now 12). **Pottery moved indigo → brass**: that tone was a literal reading of the cobalt in *Blue* Pottery, and fired earthenware groups with metal on material — brass now carries four of nine, which is what material grouping yields. `icon` is modelled (populated in all 12 rows); **`image_url` is not** (null in ten, empty string in two). Four crafts ship with **no fixtures**, safe because `gallery()` is keyed on a *product's* categoryId — the 2026-09-10 hard throw is now live rather than theoretical. `OBJECT_FORWARD` is unchanged (`['c2','c3','c4']`); the "six crafts" copy went **count-free**, because `ImpactBand` counts traditions with approved listings (5, not 9) and two numbers for one thing reads as an error. Sweep **72/72**. See `memory/decisions.md`.
- `2026-09-10` — Spiral cards **overlap by design** (~60%); the invariant is paint *order*, not separation. Corrects the "Spiral Card Separation Is a Geometric Invariant" entry above, which misdiagnosed a depth-tiebreak bug as a spacing bug and flattened the helix into a thin ribbon to fix it. Overlap is the depth cue; the real defect was two cards at identical `cos()` depth resolved by array index. `hasCardSeparation()` is replaced by `frontCardIsClear()` in `src/lib/home/spiralInvariant.ts`. See `memory/decisions.md`.
- `2026-09-11` — The auth bridge **does not mint `auth.users` rows**, inverting step 2 of `auth-migration-plan.md` §3. That step was written when `auth.users` held one row and Android authenticated against Firebase; the app has since migrated to Supabase Auth, so those five people are most likely already registered. Minting them again yields two logins per human and breaks email uniqueness on the `auth.users` side — exactly where the match-key risk lives — after which the backfill attaches a profile to whichever row the join returned and the new scoped policies enforce that mistake faithfully. The plan now *detects* first. The partial unique index `idx_profiles_auth_user_id` stays regardless, making one-profile-per-login a constraint rather than a property of a correct backfill. Planning only, nothing applied: there is no Supabase tool in-session and only the anon key locally, so the inventory (and open item #5, the `enquiries` CHECK question) is unanswered pending connector re-authorization. See `memory/auth-bridge-execution-plan.md` and `memory/decisions.md`.
- `2026-09-11` — The auth bridge is **applied**: 3 of 3 remaining profiles linked to their `auth.users` row, 0 unmapped, verified against the correct row each. The two unmatched sellers (Ananya, Nidhi) were **deleted on the owner's call** — neither of the options this plan proposed — and will re-register in the app; safe only because `products` was empty, so the FK cascade destroyed no catalogue. `firebase_uid` is gone from `profiles` entirely, which retired the exact-UID match key and the rollback trail, leaving email as the only usable key (3 clean matches, no duplicates either side, gate passed). Open item #5 is **closed**: `enquiries_buyer_or_guest_check` rejects an insert carrying neither `buyer_id` nor guest fields, proven by a rolled-back probe rather than by reading the definition, since a NULL-tolerant CHECK would have passed on `UNKNOWN`. **Phase 5 stays blocked on two things:** the three live "Seller inserts/updates/deletes own product" policies test `seller_id = auth.uid()` and are two joins short — `products.seller_id` holds a `sellers.id`, reached via `profiles.auth_user_id` then `sellers.profile_id` — so they have never matched a row and are marked for replacement, not reuse; and the real-JWT check still needs the teammate to sign into the Android app, because `unmapped = 0` proves the UPDATE ran, not that `auth.uid()` resolves. Open question to the teammate: no trigger creates a profile on `auth.users` insert, so if the app does not do it either, the two deleted sellers cannot come back. See `memory/auth-bridge-execution-plan.md` and `memory/decisions.md`.

## E · Memory Map
What lives under `/memory`:
- `project-brief.md` — the original kickoff and core mandates, frozen
- `current-strategy.md` — the *now* state and immediate focus, edited weekly
- `decisions.md` — the long-form context and reasoning behind every D entry
- `next-actions.md` — the punch list and build sequence
- `sessions/` — dated session wrap-ups (`YYYY-MM-DD-{short-slug}.md`)
- `bugs-and-risks.md` — active watch-outs, mobile performance, and edge cases

## F · References
- **Repo:** https://github.com/rishi-ventrapragada/KalaCart-Website
- **PRD:** [PRD.md](file:///d:/aiml/KalaCart-Website/PRD.md)
- **Hosting:** Vercel Dashboard

## G · Project-specific overrides & Laws
- **The 200-line law:** No source file exceeds ~200 lines. Extract subcomponents and hooks aggressively.
- **The data seam:** Never call Supabase or import mock fixtures directly in components. All access goes through `src/lib/data/`.
- **The theme seam:** Use semantic Tailwind tokens mapped to CSS variables. Never hardcode raw hex values in components.
- **The i18n seam:** No hardcoded user strings. Reference copy from `src/lib/i18n/`.
- **Three states, always:** Every data view must handle loading (skeletons), empty, and error states with retry.
- **Quality floor:** Responsive from 360px, visible focus rings, Esc-closable modals, reduced-motion respected.
- **Motion discipline:** Single easing curve `cubic-bezier(.22, 1, .36, 1)`. One Lenis rAF loop for the whole buyer shell, and the scroll engine that reads it runs on Home only (see §C). Anything the engine writes per frame carries `transition: none` **explicitly** — a zero duration is not enough, because `transition-property` defaults to `all` and one inherited duration silently reintroduces the lag. Shape binary: 999px capsule controls, 18–28px card radii.
- **Workflow & Git:** Plan before non-trivial building; inspect read-only first; verify in browser on both themes; stage explicit file paths only (never `git add .`); use heredoc commit messages.

## H · Memory Save Protocol
When explicitly asked to save, store, wrap up, or remember the conversation (e.g. "save this", "wrap this up", "remember this"):
1. Write a markdown summary to `memory/sessions/YYYY-MM-DD-{short-slug}.md` using today's date.
2. Structure: H1 title, one-line TL;DR, **What we discussed**, **What we decided**, and **What's next**.
3. Keep it punchy and concrete — no fluff.
4. Never write to `memory/sessions/` without an explicit trigger from the user in chat.

## I · External Component Adoption Protocol
Governs any component pasted in from outside this repo — reactbits, Codrops, a CodePen, a reference site, another project. External components are written against no constraints; this codebase has eight. Run every step, in order, **before** integrating. Where a step conflicts with the source component, the codebase wins.

1. **Scope.** Name the route and section it lands in. Home carries the full motion budget; every other route is reveal-only, with no continuous animation.
2. **Motion audit.** Does it start its own `requestAnimationFrame` loop or attach its own scroll listener? If it is scroll-linked and going on Home, it registers through `useScrollEngine.register()` — never a second independent scroll source alongside the Lenis singleton, which would read a different value than the rest of the page and drift out of phase. A self-contained, non-scroll-linked animation (a continuous auto-rotation, say) is lower risk, but still owes step 3.
3. **Reduced motion.** Under `prefers-reduced-motion` the loop must *stop*, not slow down and not zero its visual delta. No idling loops. Match `useScrollEngine`: compute the resting state, write it once, and never call `requestAnimationFrame` at all.
4. **Colour audit.** Every raw hex, `rgba()` and named colour mapped to an existing semantic token, verified in both themes. A genuinely new colour needs a §D decision first.
5. **Dependency check.** If it needs a package the stack does not already carry (framer-motion, GSAP, three.js), **stop and ask.** Never install silently.
6. **Data.** Real content is read through `src/lib/data/`, never the component's own fetch. If the read is async it owes the three-states rule — and if a state is deliberately omitted, say so and why rather than letting it pass unnoticed.
7. **i18n.** Every user-facing string extracted into `src/lib/i18n/`. No hardcoded copy.
8. **File-size cap.** Split to fit the ~200-line law. Logic comes out into hooks; pure maths comes out into its own module.
9. **Accessibility.** Verify keyboard operability, focus visibility, `alt` text and ARIA roles **by hand.** Do not assume the source got any of it right — it usually did not.
10. **Decision conflicts.** If adopting it reopens or contradicts a §D decision, record a new §D entry giving the reasoning, rather than silently overriding the old one.
